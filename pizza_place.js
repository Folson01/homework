let pizza = "Nebula Pizza";
let NumberOfTopping = 5;
let message = `welcome to ${pizza}, we offer up to ${NumberOfTopping} toppings on our pizza`;
if (NumberOfTopping < 10) {
  console.log("Quality, not quantity.");
}
